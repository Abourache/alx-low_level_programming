#include "main.h"
/**
 * _strlen - This function is equal to strlen
 *
 * @s: this is my entry
 *
 * Return: the las character
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0' ; i++)
	{
	}
	return (i);
}
